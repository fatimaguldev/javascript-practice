const student = {
    name: "fatima gul", 
    marks: 98.0,
}

// if we want to get perticular value of a student we can do that in the following way:

// student["name"] here the 'name' is a key
// student.name;

