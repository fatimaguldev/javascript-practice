// A "Good String" is the one which start from 'a'and has a length > 3. Write a program to check whter the string is good or not!

let str = "amn";
if ((str[0] === "a") && (str.length > 3)) {
  console.log("Good String");
} else {
     console.log("Bad String");
}