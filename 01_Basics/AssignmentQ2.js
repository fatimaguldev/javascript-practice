// take the users name and age is input using prompts then return back the following statement to the user as an alert by substituting their name and age name is a gears old use template literals to print this sentence

let userName = prompt("Enter your username");
let age = prompt("Enter your age");
console.log(`${userName}, is ${age} years old`);
